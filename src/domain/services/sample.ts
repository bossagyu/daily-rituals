/**
 * Sample utility to verify Jest setup is working correctly.
 * This file can be removed once real domain services are implemented.
 */
export function add(a: number, b: number): number {
  return a + b;
}
